interface:
  display_name: "Squad-Up To Tickets"
  short_description: "Split work into context-efficient vertical tickets"
  default_prompt: "Use $squad-up-to-tickets to split this request into safe, independently verifiable tickets."
policy:
  allow_implicit_invocation: true
